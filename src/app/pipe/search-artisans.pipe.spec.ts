import { SearchArtisansPipe } from './search-artisans.pipe';

describe('SearchArtisansPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArtisansPipe();
    expect(pipe).toBeTruthy();
  });
});
